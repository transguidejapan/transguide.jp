{
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {},
  "devDependencies": {
    "prettier": "^3.8.3",
    "prettier-plugin-go-template": "^0.0.15"
  }
}
